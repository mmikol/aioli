# What the container runs. Pinned exactly, because the image and a
# developer's checkout should be the same checkout, and with the binary extra
# so no build toolchain is needed in a slim image. The tools that check this
# code are in requirements-dev.txt beside it and are not in the image.
psycopg[binary]==3.3.6
